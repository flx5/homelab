http:
  routers:
    to-nextcloud:
      entryPoints:
        - web
      rule: "Host(`${fqdn}`)"
      service: nextcloud

  services:
    nextcloud:
      loadBalancer:
        servers:
          - url: http://${host}/